import UserHeader from '../UserHeader';
import { UserContainer } from './styles';

export default function User() {
    return (
        <UserContainer>
            <UserHeader />
        </UserContainer>
    );
}
